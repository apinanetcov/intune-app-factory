name: Intune-Printer-Factory

on:
  workflow_dispatch:
    inputs:
      printer_name:
        description: "Printer folder name under apps/Printers (e.g. HP-LJPro-M401)"
        required: true
        default: ""
  push:
    branches: [feature/printer-support]
    paths:
      - "apps/Printers/**"